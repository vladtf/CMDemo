<UserControl x:Class="CMDemo.Views.ThirdChildView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CMDemo.Views"
             mc:Ignorable="d" Background="Azure"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- First row -->
        <TextBox Grid.Row="1" HorizontalAlignment="Center" FontSize="40" Name="Person_FirstName"></TextBox>
        
        <!-- Second row -->
        <TextBox Grid.Row="2" HorizontalAlignment="Center" FontSize="40" Margin="0,20,0,0" 
                 Name="Person_LastName"/>

        <!-- Third row -->
        <Button x:Name="UpdateThis" Grid.Row="3" MinHeight="50"/>
    </Grid>
</UserControl>
